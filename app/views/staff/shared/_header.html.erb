<header>
  <%= link_to "Combee", :staff_root, class: "logo-mark" %> 
  <%= content_tag(:span, flash.notice, class: "notice") if flash.notice %>
  <%= content_tag(:span, flash.alert, class: "alert") if flash.alert %>
  <%= 
    if current_staff_member
      link_to "log out", :staff_session, method: :delete
    else 
      link_to "log in", :staff_login
    end
   %>
   <%= link_to "Account", :staff_account if current_staff_member %>
</header>
